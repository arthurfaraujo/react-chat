{
  "name": "server",
  "version": "1.0.0",
  "description": "react-chat server",
  "main": "reactChat.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tsx --watch --watch-preserve-output reactChat.ts",
    "build": "npx tsup reactChat.ts",
    "start": "node dist/reactChat.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "tsup": "^7.2.0",
    "tsx": "^3.14.0"
  }
}
