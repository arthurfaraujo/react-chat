{
  "name": "server",
  "version": "1.0.0",
  "description": "react-chat server",
  "main": "reactChat.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tsx --watch --watch-preserve-output src/reactChat.ts",
    "build": "npx tsup src/reactChat.ts --format cjs --minify --clean --sourcemap --legacy-output --target node18",
    "start": "node dist/reactChat.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.20",
    "tsup": "^7.2.0",
    "tsx": "^3.14.0",
    "typescript": "^5.2.2"
  }
}
